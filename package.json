{
  "name": "realmagents",
  "version": "1.0.0",
  "description": "RealmAgents - AI Agent Launchpad & Marketplace on Base L2",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network baseSepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
